{
  "name": "finding-islands",
  "private": true,
  "version": "1.0.0",
  "description": "Finding Islands",
  "workspaces": [
    "core",
    "parsers",
    "browser"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "format": "yarn workspaces run format"
  },
  "keywords": [
    "algorithms"
  ],
  "author": "Marcus Vinicius Monteiro de Souza",
  "license": "MIT",
  "husky": {
    "hooks": {
      "pre-commit": "yarn run test && yarn run build && yarn workspaces run format && yarn workspaces run pre-commit"
    }
  },
  "devDependencies": {
    "husky": "^2.1.0"
  }
}
