{
  "name": "finding-islands",
  "private": true,
  "version": "1.0.0",
  "description": "Finding Islands",
  "workspaces": ["core"],
  "scripts": {
    "build": "yarn workspaces run build",
    "prepublish": "yarn workspaces run prepublish",
    "publish": "yarn workspaces run publish",
    "test": "yarn workspaces run test",
    "format": "yarn workspaces run format",
    "pre-commit": "yarn run format && yarn run test"
  },
  "keywords": [
    "algorithms"
  ],
  "author": "Marcus Vinicius Monteiro de Souza",
  "license": "MIT",
  "husky": {
    "hooks": {
      "pre-commit": "yarn run pre-commit"
    }
  },
  "devDependencies": {
    "husky": "^2.1.0"
  }
}
